# tilda-custom

Custom blocks, scripts, and styles for Tilda projects.

## Structure

- `blocks/` — custom Tilda blocks
- `scripts/` — JavaScript entry points and utilities
- `styles/` — CSS overrides and custom styles
- `dist/` — build output
